# 同步所有非預算變更 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make every shared trip change sync across travelers while keeping personal budget data local.

**Architecture:** Keep the static app architecture and extend the existing Firebase Realtime Database pattern. Shared state continues to use localStorage as offline fallback, then mirrors to `rooms/<roomId>/...` when Firebase is ready.

**Tech Stack:** Vanilla JavaScript ES modules, Firebase Realtime Database, localStorage, static HTML/CSS.

---

### Task 1: Shared State Read/Write Helpers

**Files:**
- Modify: `/Users/chiyangma/bangkok-trip-companion/app.js`

- [ ] Add synced state fields for custom places, completed stops, decision votes, and random decision picks.
- [ ] Update read helpers so Firebase snapshots win when available and localStorage remains the fallback.
- [ ] Add async write helpers that save locally and write to Firebase paths when `sync.ready` is true.

### Task 2: Wire Existing Actions To Shared Writes

**Files:**
- Modify: `/Users/chiyangma/bangkok-trip-companion/app.js`

- [ ] Make stop completion write through the shared completed-stops helper.
- [ ] Make decision voting and random picks write through shared helpers.
- [ ] Make Google Maps custom candidate submission write through the shared custom-places helper.
- [ ] Leave budget expense reads/writes unchanged so personal spending stays local.

### Task 3: Firebase Listeners And Copy

**Files:**
- Modify: `/Users/chiyangma/bangkok-trip-companion/app.js`
- Modify: `/Users/chiyangma/bangkok-trip-companion/PROJECT_MEMORY.md`

- [ ] Add `onValue` listeners for custom places, completed stops, decision votes, and random decision picks.
- [ ] Re-render current UI when any shared state changes.
- [ ] Update sync banner text and project memory to say all non-budget trip changes sync.

### Task 4: Verify

**Files:**
- Inspect: `/Users/chiyangma/bangkok-trip-companion/index.html`
- Inspect: `/Users/chiyangma/bangkok-trip-companion/app.js`

- [ ] Run syntax check with `node --check app.js`.
- [ ] Start `python3 -m http.server 5173` from the project folder.
- [ ] Load `http://localhost:5173/` and confirm the app renders.
